# Phase 0-3 only: Resonate architecture, TFD feature extraction, validation,
# and checkpoint download. Install the CUDA-specific PyTorch wheel first.
torch>=2.5.1,<3.0
einops>=0.7,<1.0
huggingface_hub>=0.26,<2.0
hf_xet>=1.1,<2.0
